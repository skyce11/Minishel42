Signales

// SALIDA CTRL+C //
Par défaut, lorsque SIGINT interrompt un programme, il se termine avec un code
de sortie de 130. Ce nombre suit la convention où :
- Les codes de sortie normaux vont de 0 à 255.
- Les programmes terminés par un signal ajoutent 128 au numéro du signal reçu.
• Comme SIGINT est le signal 2, cela donne 128 + 2 = 130.

// SALIDA CTRL+\ //
Ctrl+\ envoie le signal SIGQUIT (numéro 3) au processus en cours.
SIGQUIT force le programme à s'arrêter immédiatement et génère un core dump
(une image mémoire du programme au moment de l'arrêt), ce qui est utile pour le
debugging.
- Ce qui se passe quand tu appuies sur Ctrl+\ :
    Le terminal envoie SIGQUIT au processus.
- Le programme peut le gérer (comme trap SIGQUIT dans un script) ou se terminer
    brusquement.
-  Si le programme ne capture pas SIGQUIT, il s'arrête et produit un fichier
    core qui peut être analysé avec gdb pour voir où et pourquoi il a crashé.
- Code de sortie :
    Comme pour SIGINT, le code de sortie est 128 + numéro du signal.
    SIGQUIT étant 3, le programme renverra 131 (128 + 3).

// SALIDA CTRL+D //
Ctrl+D envoie un EOF (End Of File) au terminal, ce qui signifie que l'entrée
standard (stdin) est terminée. Son effet dépend du contexte :

- Dans le terminal interactif :
Si tu appuies sur Ctrl+D, Bash interprète cela comme une fin de session et
ferme le shell (équivalent à exit).
- Dans un programme attendant une entrée :
    Par exemple, si tu utilises cat ou wc, appuyer sur Ctrl+D signifie qu'il
n'y a plus de texte à lire, donc le programme
termine son traitement.
- Dans un script :
Il est souvent utilisé pour simuler la fin d'un fichier lors d'une redirection,
par exemple :

// TEST


3. Ignorer Ctrl+\ (SIGQUIT)
✅ Objectif : Empêcher SIGQUIT de fermer le shell.

🔹 Test :

bash
cat
➡️ Appuie sur Ctrl+\ : Le shell ne doit pas être quitté, mais peut afficher "Signal ignoré".

📌 À implémenter :

signal(SIGQUIT, SIG_IGN); pour ignorer Ctrl+\.
